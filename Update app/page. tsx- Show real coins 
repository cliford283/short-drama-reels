'use client'
import { useEffect, useState } from 'react'

export default function Home() {
  const [coins, setCoins] = useState(0)

  useEffect(() => {
    // later we will get real userId from login
    setCoins(100) // demo
  }, [])

  return (
    <main className="bg-black text-white min-h-screen p-4">
      <h1 className="text-2xl font-bold text-red-500">SHORT DRAMA REELS</h1>
      <p className="mt-4">🪙 {coins} Coins</p>
      <a href="/buy-coins" className="bg-red-600 px-4 py-2 rounded mt-4 inline-block">Buy Coins</a>
      <a href="/login" className="bg-gray-700 px-4 py-2 rounded mt-4 ml-2 inline-block">Login</a>
    </main>
  )
}